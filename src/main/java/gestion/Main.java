package gestion;

import java.io.*;

public class Main implements Serializable {
    //Crear un objeto de gestion clientes para llamar a los métodos, variables y tal
    //TODO Hacer menu
    //Hay que meterlo en un try catch del menu
    /**
    //Meter imput de transpas de serializacion
    FileInputStream fis = new FileInputStream("agenda.bin");
    ObjectInputStream ois = new ObjectInputStream(fis);
    gestor = (GestorClientes)ois.readObject();
    ois.close();


    //Meter output de transpas de serializacion
    FileOutputStream fos = new FileOutputStream("agenda.bin");
    ObjectOutputStream oos = new ObjectOutputStream(fos);
    oos.writeObject(gestor);
    oos.close();

    //Meter las otras lineas
    private static final long serialVersionUID = -1411301246889842423L;**/

}
